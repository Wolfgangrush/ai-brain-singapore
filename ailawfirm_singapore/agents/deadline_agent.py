"""deadline_agent — limitation + hearing-date tracking.
PROVENANCE: STUB — v0.2+"""


def handle(payload: str) -> dict:
    """v0.1 stub. Limitation period tracking + hearing-date queries deferred to v0.2+."""
    return {
        "agent": "deadline_agent",
        "status": "v0.1 stub — limitation + hearing-date tracking deferred to v0.2+",
    }
